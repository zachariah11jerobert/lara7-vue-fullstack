<template>
    <div>
        <h1>The number is : {{ views }}</h1>
        <button @click="updateCounter(1)">Increase</button>
        <button @click="updateCounter(-1)">Decrese</button>
    </div>

    <div v-for="(blog, i) in blogs" :key="i" v-if="blogs.length">
        <h1>{{ blog.title }}</h1>
        <p>{{ blog.post }}</p>
    </div>

    <div>
        <h1>show this item if "showItem" is true</h1>
        <h1>show this item if "showItem" is false</h1>
    </div>

    <button @click="showItem!=showItem">Change showItem</button>
</template>

<script>
export default {
    data() {
        return {
            views: 0,
            blogs: [
                { title: 'this is blog title', post: 'this is blog post 1', id: 1 }
            ]
        }
    },
    methods: {
        updateCounter(number) {
            this.views += number
        }
    },
    created() {
        this.views = 100
    }
}
</script>